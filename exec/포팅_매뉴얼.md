# 7iTAX 포팅 매뉴얼

---

## 1. 빌드 및 배포 문서

### 1-1. 사용 제품 및 버전 정보

| 구분 | 제품/기술 | 버전 | 비고 |
|------|----------|------|------|
| **JVM** | Eclipse Temurin (AdoptOpenJDK) | 17 | Docker: `eclipse-temurin:17-jdk` (빌드) / `17-jre` (런타임) |
| **빌드 도구** | Gradle | 8.14.4 | Gradle Wrapper 사용 |
| **WAS** | Spring Boot Embedded Tomcat | 3.5.11 | 내장 톰캣 |
| **웹서버** | Nginx | Alpine (latest) | Docker: `nginx:alpine` |
| **AI 서비스** | Python + FastAPI + Uvicorn | Python 3.11 | Docker: `python:3.11-slim` |
| **데이터베이스** | PostgreSQL | 16 | Docker: `postgres:16` |
| **캐시** | Redis | 7 | Docker: `redis:7` |
| **벡터DB** | ChromaDB | >= 0.4.0 | AI RAG용 |
| **CI/CD** | Jenkins | - | Jenkinsfile Pipeline |
| **IDE** | IntelliJ IDEA | - | BE 개발 |
| **IDE** | Android Studio | - | FE(Android) 개발 |

#### Backend 주요 라이브러리

| 라이브러리 | 버전 | 용도 |
|-----------|------|------|
| Spring Boot | 3.5.11 | 웹 프레임워크 |
| Spring Security | (Boot 관리) | 인증/인가 |
| Spring Data JPA | (Boot 관리) | ORM |
| Spring Data Redis | (Boot 관리) | 캐시/세션 |
| jjwt | 0.12.6 | JWT 토큰 |
| Apache POI | 5.2.5 | Excel 내보내기 |
| openhtmltopdf | 1.0.10 | PDF 생성 |
| firebase-admin | 9.4.3 | FCM 푸시 알림 |
| solapi-sdk | 1.1.0 | SMS OTP |
| hypersistence-tsid | 2.1.4 | ID 생성 |
| httpclient5 | (Boot 관리) | HTTP 클라이언트 |
| Thymeleaf | (Boot 관리) | PDF 템플릿 엔진 |
| spring-retry | (Boot 관리) | 재시도 로직 (결제 알림 등) |
| spring-aspects | (Boot 관리) | AOP (Retry/Async 지원) |

#### AI 서비스 주요 라이브러리

| 라이브러리 | 버전 | 용도 |
|-----------|------|------|
| FastAPI + Uvicorn | latest | 웹 프레임워크 |
| LangChain + LangChain-OpenAI | latest | LLM 오케스트레이션 |
| ChromaDB | >= 0.4.0 | 벡터 저장소 |
| PyTorch (CPU) | >= 2.2.0 | 로컬 모델 추론 |
| Transformers | >= 4.40.0 | Reranker / 세목 분류기 |
| pypdf | >= 4.0.0 | PDF 파싱 |
| rank-bm25 | >= 0.2.2 | BM25 검색 |

#### Android (FE) 주요 설정

| 항목 | 값 |
|------|-----|
| compileSdk / targetSdk | 36 (Android 15) |
| minSdk | 28 (Android 9.0) |
| Java/Kotlin Toolchain | JDK 21 |
| Kotlin | 2.2.10 |
| Gradle | 9.3.1, AGP 9.1.0 |
| Jetpack Compose BOM | 2026.02.01 |
| Hilt | 2.57.1 |
| Retrofit + OkHttp | 2.11.0 / 4.12.0 |
| CameraX | 1.4.2 |
| ML Kit Barcode | 17.3.0 |
| Firebase BOM | 33.12.0 |
| Coil (이미지) | 3.1.0 |

---

### 1-2. 빌드 시 환경변수

#### Backend (`.env.backend`) — 배포 서버 경로: `/home/ubuntu/.env.backend`

| 환경변수 | 설명 | 기본값 | 필수 |
|----------|------|--------|------|
| `DB_HOST` | PostgreSQL 호스트 | `localhost` | O |
| `DB_PORT` | PostgreSQL 포트 | `5432` | O |
| `DB_NAME` | 데이터베이스명 | `tax_db` | O |
| `DB_USERNAME` | DB 사용자명 | `postgres` | O |
| `DB_PASSWORD` | DB 비밀번호 | (없음) | **필수** |
| `REDIS_HOST` | Redis 호스트 | `localhost` | O |
| `REDIS_PORT` | Redis 포트 | `6379` | O |
| `JWT_SECRET` | JWT 서명 키 | (없음) | **필수** |
| `AES_ENCRYPTION_KEY` | AES 암호화 키 (Base64, 32바이트) | (없음) | **필수** |
| `SSAFY_FINANCE_API_KEY` | SSAFY 금융망 API 키 | (없음) | **필수** |
| `SSAFY_FINANCE_USER_KEY` | SSAFY 금융망 사용자 키 | (빈 값) | 선택 |
| `SOLAPI_API_KEY` | Solapi SMS API 키 | (없음) | **필수** |
| `SOLAPI_API_SECRET` | Solapi SMS API 시크릿 | (없음) | **필수** |
| `SOLAPI_SENDER_PHONE` | SMS 발신번호 | (없음) | **필수** |
| `AI_SERVICE_BASE_URL` | AI 서비스 URL | `http://localhost:8000` | O |
| `CORS_ALLOWED_ORIGINS` | CORS 허용 오리진 | `http://localhost:3000` | O |
| `FCM_ENABLED` | FCM 활성화 여부 | `false` | 선택 |
| `FCM_SERVICE_ACCOUNT_PATH` | Firebase 서비스 계정 JSON 경로 | (빈 값) | FCM 사용 시 필수 |
| `TEST_LOGIN_ENABLED` | 테스트 로그인 활성화 | `false` | 선택 |

#### AI 서비스 (`.env.ai`) — 배포 서버 경로: `/home/ubuntu/.env.ai`

| 환경변수 | 설명 | 기본값 | 필수 |
|----------|------|--------|------|
| `GMS_API_KEY` | GMS 프록시 API 키 (OpenAI 접근용) | (없음) | **필수** |
| `GMS_BASE_URL` | GMS 프록시 베이스 URL | `https://gms.ssafy.io/gmsapi/api.openai.com/v1` | O |
| `LLM_MODEL` | 기본 LLM 모델 | `gpt-4o-mini` | O |
| `LLM_MODEL_MINI` | 경량 모델 | `gpt-4o-mini` | O |
| `LLM_MODEL_STANDARD` | 표준 모델 | `gpt-4o` | O |
| `RAG_ENABLED` | RAG 기능 활성화 | `true` | 선택 |
| `CHROMA_PERSIST_DIRECTORY` | ChromaDB 영속 경로 | `./data/chroma` | O |
| `EMBEDDING_MODEL` | 임베딩 모델 | `text-embedding-3-small` | O |
| `CACHE_ENABLED` | 시맨틱 캐시 활성화 | `true` | 선택 |
| `CACHE_THRESHOLD` | 캐시 유사도 임계값 | `0.95` | 선택 |
| `CACHE_MAX_ENTRIES` | 캐시 최대 엔트리 수 | `10000` | 선택 |
| `CACHE_TTL_HOURS` | 캐시 TTL(시간) | `24` | 선택 |
| `BACKEND_BASE_URL` | 백엔드 API URL | `http://localhost:8080` | O |
| `BACKEND_API_KEY` | 백엔드 API 키 | (빈 값) | 선택 |
| `DEBUG` | 디버그 모드 | `false` | 선택 |

#### Android (FE) 빌드 설정

| 항목 | 값 |
|------|-----|
| API Base URL | `https://j14c203.p.ssafy.io/api/` |
| WebView Base URL | `https://j14c203.p.ssafy.io` |
| Firebase 프로젝트 | `google-services.json` 교체 필요 (현재 placeholder) |
| 서명 키 | 별도 keystore 파일 필요 (release 빌드 시) |

---

### 1-3. 배포 시 특이사항

#### 아키텍처 개요

```
[Android App] ──HTTPS──→ [Nginx :3000] ──/api/*──→ [Spring Boot :18080]
                              │                          │
                              │ 정적 파일 서빙            ├──→ [PostgreSQL :5432]
                              │ (React SPA)              ├──→ [Redis :6379]
                              │                          └──→ [AI Service :19000]
                              │                                    │
                              │                              [GMS/OpenAI]
                              │                              [ChromaDB]
```

#### 로컬 개발 환경 (docker-compose.yml)

`docker-compose.yml`로 인프라(DB, Redis, AI, Worker)를 일괄 기동합니다.

```bash
# 로컬 인프라 기동
GMS_API_KEY=<키> AES_ENCRYPTION_KEY=<키> docker compose up -d
```

| 서비스 | 이미지 | 컨테이너명 | 포트 | 비고 |
|--------|--------|-----------|------|------|
| PostgreSQL | `postgres:16` | `tax7i-postgres` | 5432 | DB: `tax7i`, User: `ssafy`, PW: `ssafy` |
| Redis | `redis:7` | `tax7i-redis` | 6379 | PW: `ssafy` |
| AI Service | `./ai` (빌드) | `tax7i-ai` | 8000 | GMS_API_KEY 필수 |
| Worker | `./BE` (빌드) | `tax7i-worker` | - | Spring Profile: `worker` |

- PostgreSQL 초기화: `db/init/` 디렉토리의 SQL 파일이 `docker-entrypoint-initdb.d`에 마운트되어 최초 기동 시 자동 실행
- Worker 서비스: `SPRING_PROFILES_ACTIVE=worker`로 실행, DB/Redis/AI에 의존 (healthcheck 통과 후 기동)
- AI → Backend 통신: `http://host.docker.internal:8080` (로컬 Spring Boot와 연동)

#### 헬스체크

| 서비스 | 체크 명령 | 간격 |
|--------|----------|------|
| PostgreSQL | `pg_isready -U ssafy -d tax7i` | 10s, 3회 재시도 |
| Redis | `redis-cli -a ssafy ping` | 10s, 3회 재시도 |
| AI Service | HTTP GET `/api/v1/health/` | 15s, 3회 재시도, 30s 대기 |

#### 영속 볼륨 (docker-compose)

| 볼륨명 | 마운트 경로 | 용도 |
|--------|-----------|------|
| `postgres-data` | `/var/lib/postgresql/data` | DB 데이터 |
| `redis-data` | `/var/lib/redis/data` | Redis 데이터 |
| `ai-chroma-data` | `/app/data/chroma` | ChromaDB 벡터 데이터 |

#### 운영 배포 환경 (Jenkinsfile)

Jenkins Pipeline으로 Docker 이미지 빌드 후 개별 `docker run` 실행합니다.

**배포 순서:**

```
1. BE Docker Build   → tax-backend 이미지 빌드
2. FE Docker Build   → tax-frontend 이미지 빌드
3. Prepare AI Model  → /opt/tax7i/models/tax_classifier → ai/models/ 복사
4. AI Docker Build   → tax-ai 이미지 빌드
5. Deploy            → 기존 컨테이너 정지/삭제 → 새 컨테이너 실행
```

**운영 포트 매핑:**

| 서비스 | 컨테이너 내부 포트 | 호스트 외부 포트 | 컨테이너명 |
|--------|-------------------|-----------------|-----------|
| Backend (Spring Boot) | 8080 | **18080** | `tax-backend` |
| Frontend (Nginx) | 80 | **3000** | `tax-frontend` |
| AI Service (Uvicorn) | 8000 | **19000** | `tax-ai` |

- Docker 네트워크: `ubuntu_default` (컨테이너 간 통신)
- 타임존: Backend `TZ=Asia/Seoul`
- 환경변수 파일: `/home/ubuntu/.env.backend`, `/home/ubuntu/.env.ai`

**운영 볼륨 마운트:**

| 컨테이너 | 호스트 경로 | 컨테이너 경로 | 용도 |
|----------|-----------|-------------|------|
| tax-backend | `/home/ubuntu/firebase-service-account.json` | 동일 경로 (read-only) | FCM 서비스 계정 |
| tax-ai | `chroma-data` (Docker Volume) | `/app/data/chroma` | ChromaDB 데이터 |

**AI 모델 파일:**
- 빌드 전 `/opt/tax7i/models/tax_classifier`에 모델 배치 필요
- Jenkins가 자동으로 `ai/models/tax_classifier/`로 복사
- 미배치 시 LLM 기반 분류만 사용 (분류기 없이 동작)

#### Nginx 리버스 프록시 (`FE/nginx.conf`)

```nginx
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;   # SPA 라우팅
    }

    location /api/ {
        proxy_pass http://tax-backend:8080;  # 백엔드 프록시
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

#### Spring 비동기/스케줄링/재시도 설정

| 설정 | 내용 |
|------|------|
| `@EnableAsync` | 비동기 처리 활성화 (ThreadPoolTaskExecutor: core 2, max 5, queue 50) |
| `@EnableRetry` | 재시도 로직 활성화 (결제 알림 등에 사용) |
| `@EnableScheduling` | 스케줄링 활성화 (`TaxCalendarScheduler` — 세금 기한 알림) |
| 이벤트 리스너 | `BookEntryEventListener` (장부 자동 생성), `PaymentNotificationListener` (결제 알림) |

#### Redis 캐시 설정

| 캐시명 | TTL | 용도 |
|--------|-----|------|
| `mccTaxRules` | 1시간 | MCC 세목 규칙 |
| `merchants` | 1시간 | 가맹점 정보 |
| `taxDeadlines` | 24시간 | 세금 기한 |
| `entertainmentUsed` | 5분 | 접대비 사용액 |
| `taxBrackets` | 24시간 | 세율 구간 |
| `taxParameters` | 24시간 | 세금 파라미터 |

#### Security 공개 엔드포인트 (JWT 불필요)

- `/api/auth/**` — 인증 관련
- `/api/tax-calendar/**` — 세금 캘린더
- `/api/payments/qr/merchant-token/**` — 가맹점 QR 결제

#### 빌드 알림

- Mattermost Webhook으로 빌드 성공/실패 자동 알림

---

### 1-4. DB 접속 정보 및 프로퍼티 파일 목록

#### DB 접속 정보

| 항목 | 로컬 (docker-compose) | 운영 (Jenkinsfile) |
|------|----------------------|-------------------|
| DBMS | PostgreSQL 16 | PostgreSQL 16 |
| 호스트 | `localhost` | `${DB_HOST}` |
| 포트 | `5432` | `${DB_PORT}` (기본: 5432) |
| 데이터베이스명 | `tax7i` | `${DB_NAME}` (기본: `tax_db`) |
| 사용자명 | `ssafy` | `${DB_USERNAME}` (기본: `postgres`) |
| 비밀번호 | `ssafy` | `${DB_PASSWORD}` (필수) |
| DDL 전략 | `update` (자동 스키마 갱신) | `update` |
| 초기화 SQL | `db/init/` (5개 파일, 최초 기동 시 자동 실행 — 필수 4개 + 선택 1개) | - |

#### 주요 프로퍼티/설정 파일 목록

| 파일 경로 | 용도 |
|----------|------|
| `BE/src/main/resources/application.yaml` | BE 메인 설정 (DB, Redis, JWT, 외부 서비스 등) |
| `BE/build.gradle` | BE 빌드 설정 및 의존성 |
| `BE/settings.gradle` | 프로젝트명 (`tax7i`) |
| `BE/Dockerfile` | BE Docker 멀티스테이지 빌드 |
| `FE/app/build.gradle.kts` | Android 앱 빌드 설정 |
| `FE/gradle/libs.versions.toml` | Android 의존성 버전 카탈로그 |
| `FE/app/google-services.json` | Firebase 설정 (placeholder) |
| `FE/app/src/.../util/Constants.kt` | API Base URL 상수 |
| `FE/Dockerfile` | FE(Web) Docker 빌드 (Node 20 + Nginx) |
| `FE/nginx.conf` | Nginx 리버스 프록시 설정 |
| `FE/package.json` | Web FE 의존성 (React 19, Vite 8) |
| `ai/.env.example` | AI 서비스 환경변수 템플릿 |
| `ai/Dockerfile` | AI 서비스 Docker 빌드 |
| `ai/requirements.txt` | Python 의존성 목록 |
| `docker-compose.yml` | 로컬 개발 인프라 (PostgreSQL, Redis, AI, Worker) |
| `Jenkinsfile` | CI/CD 파이프라인 |
| `db/init/01_schema.sql` | DB 스키마 (22개 테이블) — **필수** |
| `db/init/02_seed_data.sql` | 초기 시드 데이터 — **필수** |
| `db/init/03_sync_tax_seed_data.sql` | 2025년 세금 기준 데이터 동기화 (멱등) — **필수** |
| `db/init/04_test_book_entries.sql` | 테스트 결제내역 (세목별 검증용) — 선택 (시연용) |
| `db/init/05_fix_book_entry_amounts.sql` | 장부 금액 보정 (공급가액→결제금액) — **필수** |
| `BE/src/main/resources/templates/excel/` | Excel 템플릿 (income_tax_template.xlsx, vat_template.xlsx) |
| `BE/src/main/resources/templates/pdf/` | PDF 템플릿 (simple-ledger.html, tax-receipt.html, tax-return.html) |

---

## 2. 외부 서비스 정보

### 2-1. SSAFY 금융망 API (필수)

| 항목 | 값 |
|------|-----|
| 서비스명 | SSAFY Finance OpenAPI |
| 용도 | 수시입출금 계좌 관리, 이체, 거래내역 조회, 신용카드 결제 |
| Base URL | `https://finopenapi.ssafy.io/ssafy/api/v1/edu` |
| 회원 API URL | `https://finopenapi.ssafy.io/ssafy/api/v1/member/` |
| 필요 키 | `SSAFY_FINANCE_API_KEY` |
| 가입/발급 | SSAFY 교육용 금융 시뮬레이션 API (교육생 제공) |
| Institution Code | `00100` |
| Fintech App No | `001` |

### 2-2. GMS — OpenAI 프록시 (필수)

| 항목 | 값 |
|------|-----|
| 서비스명 | GMS (SSAFY OpenAI 프록시) |
| 용도 | AI 챗봇 답변 생성, 거래 자동 분류 |
| Base URL | `https://gms.ssafy.io/gmsapi/api.openai.com/v1` |
| 필요 키 | `GMS_API_KEY` |
| 사용 모델 | `gpt-4o-mini` (기본), `gpt-4o` (표준) |
| 가입/발급 | SSAFY GMS 포털에서 API 키 발급 |

### 2-3. Solapi SMS (필수)

| 항목 | 값 |
|------|-----|
| 서비스명 | Solapi (솔라피) |
| 용도 | SMS OTP 인증 (본인인증 시 6자리 인증번호 발송) |
| 공식 사이트 | https://solapi.com |
| 필요 키 | `SOLAPI_API_KEY`, `SOLAPI_API_SECRET`, `SOLAPI_SENDER_PHONE` |
| SDK | `solapi-sdk:1.1.0` (Java) |
| 가입 방법 | Solapi 회원가입 → 앱 생성 → API 키 발급 → 발신번호 등록 |

### 2-4. Firebase Cloud Messaging (선택)

| 항목 | 값 |
|------|-----|
| 서비스명 | Firebase (Google) |
| 용도 | 모바일 푸시 알림 (거래 분류 완료, 세금 기한 알림) |
| 설정 파일 (BE) | `firebase-service-account.json` (서비스 계정 키) |
| 설정 파일 (FE) | `google-services.json` (Firebase 프로젝트 설정) |
| SDK (BE) | `firebase-admin:9.4.3` |
| SDK (FE) | `firebase-messaging` (Firebase BOM 33.12.0) |
| 활성화 | `FCM_ENABLED=true` 환경변수 설정 |
| 가입 방법 | Firebase Console → 프로젝트 생성 → Android 앱 등록 → 서비스 계정 키 다운로드 |

### 2-5. 외부 서비스 요약 다이어그램

```
                    ┌─────────────────────┐
                    │     Android App     │
                    └──────────┬──────────┘
                               │ HTTPS
                    ┌──────────▼──────────┐
                    │   Nginx (Frontend)  │──── 정적 파일 서빙 (React SPA)
                    └──────────┬──────────┘
                               │ /api/*
                    ┌──────────▼──────────┐
              ┌─────│  Spring Boot (BE)   │─────┐
              │     └──────────┬──────────┘     │
              │                │                │
     ┌────────▼─────┐  ┌──────▼──────┐  ┌──────▼──────┐
     │  PostgreSQL   │  │    Redis    │  │  AI Service │
     │  16           │  │    7        │  │  (FastAPI)  │
     └───────────────┘  └─────────────┘  └──────┬──────┘
                                                │
                                         ┌──────▼──────┐
                                         │ GMS(OpenAI) │
                                         │ ChromaDB    │
                                         └─────────────┘
외부 API:
  ├── SSAFY Finance API (금융망)
  ├── Solapi (SMS OTP)
  └── Firebase (FCM 푸시)
```

---

## 3. DB 덤프 파일

### 초기화 SQL 파일 (5개)

| 순서 | 파일 | 설명 | 필수 여부 |
|------|------|------|----------|
| 1 | `db/init/01_schema.sql` | 22개 테이블 DDL | **필수** |
| 2 | `db/init/02_seed_data.sql` | 초기 시드 데이터 (가맹점 84개, 카드 상품, 테스트 계정) | **필수** |
| 3 | `db/init/03_sync_tax_seed_data.sql` | 2025년 세금 기준 데이터 동기화 (세율 구간 8개, 기한 6개, 파라미터) — 멱등성 보장 | **필수** |
| 4 | `db/init/04_test_book_entries.sql` | 테스트 결제내역 (1인 IT개발자 간편장부, 20개 세목 커버) | 선택 (시연용) |
| 5 | `db/init/05_fix_book_entry_amounts.sql` | 장부 금액 보정 (공급가액 → 원래 결제금액 복원) | **필수** |

> `04`는 시연 데모용 테스트 데이터입니다. 시연 없이 운영만 할 경우 생략 가능합니다.

- `docker-compose.yml` 사용 시 최초 기동 때 `db/init/` 디렉토리가 파일명 순서(01→05)로 자동 실행됨
- 운영 DB에서 `03~05`는 수동 실행 필요: `psql -U ssafy -d tax7i -f <파일>`
- JPA DDL 전략: `hibernate.ddl-auto: update` (앱 기동 시 스키마 자동 갱신)
- 운영 DB 덤프가 필요한 경우:

```bash
pg_dump -h <DB_HOST> -U ssafy -d tax7i --no-owner --no-privileges > exec/db_dump.sql
```

---

## 4. 시연 시나리오

### 시나리오 1: 회원가입 및 로그인

| 순서 | 화면 | 실행 내용 |
|------|------|----------|
| 1 | 스플래시 | 앱 실행 → 스플래시 화면 표시 |
| 2 | 본인인증 | 이름, 주민등록번호 앞자리, 통신사, 휴대폰번호 입력 → "인증 요청" 클릭 |
| 3 | SMS 인증 | 수신된 6자리 인증번호 입력 → "확인" 클릭 |
| 4 | 약관 동의 | 필수 약관 전체 동의 체크 → "동의하고 계속" 클릭 |
| 5 | PIN 설정 | 6자리 PIN 번호 입력 |
| 6 | PIN 확인 | 동일한 6자리 PIN 재입력 → 가입 완료 |
| 7 | 가입 성공 | 성공 화면 확인 → "시작하기" 클릭 |
| 8 | PIN 로그인 | 이후 접속 시 6자리 PIN 입력으로 로그인 |

### 시나리오 2: 카드 등록

| 순서 | 화면 | 실행 내용 |
|------|------|----------|
| 1 | 홈 | 하단 탭 바에서 "카드" 메뉴 선택 |
| 2 | 카드 목록 | "카드 등록하기" 버튼 클릭 |
| 3 | 카드 유형 선택 | 신용카드/체크카드 선택 |
| 4 | 카드 정보 입력 | 카드번호, 유효기간, CVC 입력 |
| 5 | 카드 상품 선택 | SSAFY 카드 상품 선택 |
| 6 | 출금 계좌 선택 | 결제 대금 출금 계좌 선택 |
| 7 | 사업자 정보 | 사업자등록번호, 상호명 입력 |
| 8 | 본인 확인 | 카드 소유자 본인 확인 |
| 9 | SMS 인증 | 카드 등록 SMS 인증번호 입력 |
| 10 | 카드 등록 완료 | 등록 완료 확인 |

### 시나리오 3: Pay 등록 및 QR 결제

| 순서 | 화면 | 실행 내용 |
|------|------|----------|
| 1 | 홈 | "Pay" 메뉴 선택 |
| 2 | Pay 소개 | 서비스 소개 확인 → "시작하기" 클릭 |
| 3 | Pay 약관 동의 | 이용 약관 동의 |
| 4 | Pay 본인 인증 | 본인 인증 진행 |
| 5 | Pay 사업자 정보 | 사업자 정보 입력/확인 |
| 6 | Pay 확인 | 등록 정보 최종 확인 → "등록" 클릭 |
| 7 | Pay 등록 완료 | Pay 등록 성공 확인 |
| 8 | 홈 | "QR 결제" 메뉴 선택 |
| 9 | QR 결제 | 카메라 활성화 → 가맹점 QR 코드 스캔 |
| 10 | 결제 처리 | 결제 금액 확인 → "결제하기" 클릭 |
| 11 | 결제 완료 | 결제 성공 화면 확인 |

### 시나리오 4: 자동 장부 분류 및 조회

| 순서 | 화면 | 실행 내용 |
|------|------|----------|
| 1 | 홈 | "장부" 메뉴 선택 |
| 2 | 장부 목록 | 자동 생성된 장부 항목 확인 (결제 시 자동 생성) |
| 3 | 미분류 항목 | "미분류" 필터 선택 → 미분류 거래 확인 |
| 4 | AI 자동 분류 | "자동 분류" 버튼 클릭 → AI 분류 로딩 |
| 5 | 분류 결과 | AI 분류 결과 확인 (카테고리, 신뢰도) |
| 6 | 카테고리 수정 | 필요 시 카테고리 수동 변경 |
| 7 | 메모 추가 | 거래에 메모 추가 → "저장" 클릭 |
| 8 | 장부 상세 | 장부 항목 상세 내용 확인 (VAT 공제 여부, 사업용/개인용) |

### 시나리오 5: 세금 신고 및 절세

| 순서 | 화면 | 실행 내용 |
|------|------|----------|
| 1 | 홈 | "리포트" 메뉴 선택 |
| 2 | 세금 리포트 | 종합소득세/부가세 요약 확인 |
| 3 | 절세 추천 | AI 기반 절세 팁 확인 |
| 4 | 세금 캘린더 | 하단 탭 "캘린더" → 납부 기한 목록 확인 |
| 5 | 캘린더 상세 | 특정 기한 클릭 → 상세 내용 확인 |
| 6 | 알림 설정 | 납부 기한 알림 ON/OFF 설정 |

### 시나리오 6: AI 챗봇 상담

| 순서 | 화면 | 실행 내용 |
|------|------|----------|
| 1 | 하단 탭 | AI 챗봇 아이콘 선택 |
| 2 | AI 챗봇 | "접대비 한도가 얼마인가요?" 등 세금 관련 질문 입력 |
| 3 | AI 챗봇 | AI 응답 확인 (RAG 기반 세법 지식 + LLM 답변) |
| 4 | AI 챗봇 | 추가 질문 계속 → 대화 히스토리 유지 |

### 시나리오 7: 데이터 내보내기

| 순서 | 화면 | 실행 내용 |
|------|------|----------|
| 1 | 장부 목록 | "내보내기" 버튼 클릭 |
| 2 | 내보내기 목적 | 내보내기 용도 선택 |
| 3 | 기간 설정 | 시작일/종료일 선택 |
| 4 | 형식 선택 | 내보내기 형식 선택 → "내보내기" 클릭 |
| 5 | 파일 저장 | 생성된 파일 다운로드/공유 |
