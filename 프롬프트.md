
# 7iTAX 미구현 15개 — 세법 검증 완료 Claude CLI 프롬프트

> 세법 근거가 필요한 항목(#5, #8, #11, #13, #14)은 조문 확인 완료.
> 한 번에 하나씩만 CLI에 넘길 것. 순서대로 실행.

---

## #1. entryType 필터 [세법 불필요]

```
프로젝트에서 다음을 순서대로 읽어줘:
1. BookEntry 엔티티 (필드 전체, 특히 entryType 또는 type 필드)
2. EntryType enum (있다면)
3. BookEntryController (GET /api/book-entries 메서드)
4. BookEntryRepository (기존 쿼리 메서드들)
5. BookEntryService (목록 조회 메서드)

확인한 뒤, GET /api/book-entries에 entryType 필터 파라미터를 추가해줘.

요구사항:
- 파라미터: entryType (String, optional)
- 값: EntryType enum에 정의된 값들 — enum을 확인한 뒤 그 값을 사용
- null이면 전체 조회 (기존 동작 유지)
- 기존 confirmed 필터와 AND 조합
- Repository 쿼리 방식은 기존 패턴을 따를 것 (Specification이면 Specification, @Query면 @Query)
- Controller 파라미터 추가 시 기존 스타일 유지

변경 파일 목록과 각 파일의 변경 내용을 보여줘.
테스트용 curl 명령어도 같이 줘.
```

---

## #2. 날짜 범위 필터 [세법 불필요]

```
프로젝트에서 다음을 읽어줘:
1. BookEntry 엔티티 — 날짜 관련 필드 전부 확인 (transactionDate, tradedAt, createdAt 등)
2. BookEntryController — GET /api/book-entries의 현재 파라미터
3. BookEntryRepository — 기존 쿼리 패턴

"거래일"을 나타내는 필드를 찾은 뒤, GET /api/book-entries에 날짜 범위 필터를 추가해줘.

요구사항:
- 파라미터: startDate (LocalDate, optional), endDate (LocalDate, optional)
- 거래일 필드 기준으로 필터 (createdAt이 아니라 실제 거래일)
- startDate만 있으면: 거래일 >= startDate
- endDate만 있으면: 거래일 <= endDate
- 둘 다 있으면: startDate <= 거래일 <= endDate
- 둘 다 없으면: 기존 동작 (전체)
- #1에서 추가한 entryType, 기존 confirmed와 AND 조합
- 기존 Repository 쿼리 패턴 따를 것

변경 파일 목록과 curl 테스트 명령어 포함.
```

---

## #3. 카테고리 필터 [세법 참고]

세목코드 참고 (국세청고시 제2024-19호):
11 매입비용 / 14 임차료 / 16 여비교통비 / 17 접대비 / 18 보험료 / 20 소모품비 / 21 지급수수료 / 22 차량유지비 / 23 복리후생비 / 24 통신비 / 25 도서인쇄비 / 26 광고선전비 / 27 연구비 / 30 감가상각비

```
프로젝트에서 다음을 순서대로 읽어줘:
1. BookEntry 엔티티 — 카테고리/세목 관련 필드 (category, taxCategory, categoryCode 등)
2. TaxCategory 엔티티 또는 enum (있다면)
3. MccTaxRule 엔티티
4. BookEntryController — GET /api/book-entries 파라미터
5. BookEntryRepository

카테고리를 나타내는 필드와 그 값의 형태(enum, String, FK 등)를 확인한 뒤,
GET /api/book-entries에 카테고리 필터를 추가해줘.

요구사항:
- 파라미터: category (기존 카테고리 필드 타입에 맞춤)
- 해당 카테고리의 장부 항목만 필터
- 기존 필터들(confirmed, entryType, startDate~endDate)과 AND 조합
- 기존 쿼리 패턴 따를 것

변경 파일 목록과 curl 테스트 명령어 포함.
```

---

## #4. 거래처 검색 [세법 불필요]

```
프로젝트에서 다음을 읽어줘:
1. BookEntry 엔티티 — 거래처/가맹점 관련 필드 (merchantName, description 등)
2. BookEntryController — GET /api/book-entries
3. BookEntryRepository

거래처/가맹점명 필드를 찾은 뒤, GET /api/book-entries에 검색 파라미터를 추가해줘.

요구사항:
- 파라미터: keyword (String, optional)
- 가맹점명 필드에 LIKE '%keyword%' (대소문자 무시, 한글 포함)
- keyword가 null이거나 빈 문자열이면 무시
- 기존 필터들과 AND 조합
- DB가 PostgreSQL이면 ILIKE 사용
- 기존 쿼리 패턴 따를 것

변경 파일 목록과 curl 테스트 명령어 포함.
```

---

## #5. 증빙 필터 [세법 검증 완료]

세법 근거: 소득세법 시행령 제208조의2 (적격증빙)
적격증빙 4종: 세금계산서, 계산서, 신용카드매출전표, 현금영수증
비적격: 간이영수증, 거래명세서, 계좌이체 내역

```
프로젝트에서 BookEntry 엔티티를 읽어줘.
증빙 관련 필드가 있는지 확인 (hasReceipt, evidenceType, receiptUrl, paymentMethod 등).

GET /api/book-entries에 증빙 필터를 추가해줘.

세법 기준 (적격증빙 — 소득세법 시행령 제208조의2):
적격증빙은 세금계산서, 계산서, 신용카드매출전표, 현금영수증 4종만 해당.
간이영수증, 계좌이체는 비적격.

판단 로직 (BookEntry 필드에 맞춰 적용):
- 결제수단이 카드(CARD)면 → 적격 (신용카드매출전표)
- 현금영수증 발급 건이면 → 적격
- 세금계산서 연결 건이면 → 적격
- 위에 해당 안 되면 → 비적격

요구사항:
- 파라미터: hasEvidence (Boolean, optional)
- true: 적격증빙 있는 거래만
- false: 적격증빙 없는 거래만
- null: 전체
- 기존 필터들과 AND 조합
- 기존 쿼리 패턴 따를 것

만약 증빙 판단할 필드가 없으면, paymentMethod == CARD를 적격증빙으로 간주하고 주석으로 근거 표시.
```

---

## #6. 미분류 필터 [세법 불필요]

```
프로젝트에서 BookEntry 엔티티를 읽어줘.
카테고리/세목 분류를 나타내는 필드 확인.

GET /api/book-entries에 미분류 필터를 추가해줘.

요구사항:
- 파라미터: unclassified (Boolean, optional)
- true: 카테고리 필드가 null이거나 기본값인 거래만
- false: 분류 완료된 거래만
- null: 전체
- 주의: confirmed(사용자 확인 여부)와는 다른 필터임
- 기존 필터들과 AND 조합
- 기존 쿼리 패턴 따를 것
```

---

## #7. 메모 수정 API [세법 불필요]

```
프로젝트에서 BookEntryController의 기존 PATCH 엔드포인트를 전부 읽어줘.
(confirm, category, personal, business 등)

같은 패턴으로 메모 수정 API를 추가해줘.

엔드포인트: PATCH /api/book-entries/{entryId}/note
Request Body: { "note": "거래처 미팅 점심" }

요구사항:
1. 기존 PATCH 메서드와 동일한 구조
2. 소유자 검증 (다른 사용자 장부 수정 불가)
3. note가 null이거나 빈 문자열이면 메모 삭제
4. note 길이 200자 이내 (@Size validation)
5. 응답은 기존 PATCH 응답 패턴과 동일
```

---

## #8. 지방소득세 CSV [세법 검증 완료]

세법 근거:
- 지방세법 제92조: 개인지방소득세 세율 = 소득세 적용세율의 1/10
- 지방세법 제95조: 종합소득세 확정신고 시 동시 신고·납부
- 신고 기한: 귀속연도 다음해 5.1~5.31
- 납부처: 납세지 관할 지방자치단체 (위택스)

```
프로젝트에서 ExportService와 ExportController를 읽어줘.
기존 CSV 내보내기(book-entries, vat, income-tax) 패턴 확인.

같은 패턴으로 지방소득세 CSV 내보내기를 추가해줘.

엔드포인트: GET /api/export/local-tax?year=2025

세법 근거:
- 지방세법 제92조: 개인지방소득세 세율 = 소득세 적용세율의 1/10
- 지방세법 제95조: 종합소득세 확정신고 시 동시 신고·납부
- 신고기한: {year+1}년 5월 1일 ~ 5월 31일
- 납부처: 납세지 관할 지방자치단체 (위택스)

CSV 필드:
- 귀속연도
- 성명
- 사업자등록번호
- 종합소득세 결정세액 (기존 TaxEstimationService에서 가져오기)
- 개인지방소득세 = 종합소득세 결정세액 × 10% (원 단위 절사, Math.floor)
- 신고기한: "{year+1}년 5월 31일"
- 납부처: "납세지 관할 지방자치단체 (위택스)"
- 법적근거: "지방세법 제92조, 제95조"

주의: 지방소득세 = 결정세액의 10%이지, 납부세액의 10%가 아님.
파일명: 지방소득세_{year}.csv
기존 ExportService 패턴을 정확히 따를 것.
```

---

## #9. 내보내기 날짜 범위 [세법 불필요]

```
프로젝트에서 ExportController와 ExportService를 읽어줘.
GET /api/export/book-entries의 기존 파라미터와 로직 확인.

날짜 범위 파라미터를 추가해줘.

요구사항:
- 추가 파라미터: startDate (LocalDate, optional), endDate (LocalDate, optional)
- startDate/endDate가 있으면 year 무시, 해당 범위로 필터
- 둘 다 없으면 기존처럼 year 기준
- 파일명: 범위가 있으면 간편장부_{startDate}_{endDate}.csv, 없으면 간편장부_{year}.csv
- 기존 로직 최소 변경
```

---

## #10. Excel 내보내기 [세법 불필요]

```
프로젝트에서 다음을 읽어줘:
1. ExportService — 기존 CSV 생성 로직
2. build.gradle — 현재 의존성

Excel(.xlsx) 내보내기를 추가해줘.

1단계 - 의존성:
build.gradle에: implementation 'org.apache.poi:poi-ooxml:5.2.5'

2단계 - 파라미터:
GET /api/export/book-entries?year=2025&format=xlsx
- format=csv (기본값)
- format=xlsx (새로 추가)

3단계 - xlsx 생성:
- 시트명: "간편장부"
- 헤더: 날짜 | 거래내용 | 거래처 | 수입금액 | 비용금액 | 고정자산 | 카테고리 | 비고
- 헤더 스타일: 배경 #4472C4, 흰색 글씨, bold
- 금액 셀: #,##0 포맷
- 열 너비: autoSizeColumn
- 하단 합계 행

4단계 - 응답:
- Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
- filename: 간편장부_{year}.xlsx

기존 CSV 데이터 조회 로직 재사용, 출력만 분기.
```

---

## #11. PDF 장부 리포트 [세법 검증 완료]

세법 근거:
- 국세청고시 제2024-19호: 간편장부 서식 (수입/비용을 가계부처럼 작성)
- 국세기본법 제85조의3: 장부 및 증빙서류 5년 보관 의무
- 간편장부 칼럼: 일자, 거래내용, 거래처, 수입금액(매출/기타), 비용(매입/경비), 고정자산(증가/감소), 비고

```
프로젝트에서 ExportService와 build.gradle을 읽어줘.
PDF 관련 의존성이 있는지 확인 (openhtmltopdf, itext, openpdf 등).
기존 PDF 생성 로직이 있으면 그 패턴을 따라.

장부 PDF 내보내기를 추가해줘.

1단계 - 의존성 (기존에 없으면 추가):
implementation 'com.openhtmltopdf:openhtmltopdf-pdfbox:1.0.10'

2단계 - 엔드포인트:
GET /api/export/book-entries?year=2025&format=pdf

3단계 - PDF 내용 (국세청고시 제2024-19호 간편장부 서식 준용):
제목: "간편장부 ({year}년)"
사업자 정보: 성명, 사업자등록번호, 업종코드, 주소
테이블 칼럼 (국세청 서식 기준):
  일자 | 거래내용 | 거래처 | 수입금액(매출) | 수입금액(기타) | 비용(매입) | 비용(경비) | 고정자산(증가) | 고정자산(감소) | 비고
하단 합계행: 각 금액 칼럼 합계
푸터: "국세기본법 제85조의3에 따라 확정신고기한이 지난 날부터 5년간 보관하여야 합니다."

4단계 - 한글 폰트:
resources/fonts/에 NotoSansKR-Regular.ttf 배치
HTML @font-face로 지정
Docker에도 폰트 포함

5단계 - 구현:
HTML 문자열(또는 Thymeleaf 템플릿) → openhtmltopdf로 PDF 변환
기존 ExportService 패턴 따를 것.
```

---

## #12. 전년 대비 비교 [세법 불필요]

```
프로젝트에서 BookEntryService의 summary 관련 메서드를 읽어줘.
연간 요약(totalIncome, totalExpense 등)을 반환하는 부분.

전년 대비 비교 데이터를 추가해줘.

요구사항:
1. 요청된 year의 summary + (year-1)의 summary를 둘 다 조회
2. 응답에 다음 필드 추가:
   - previousYear: { totalIncome, totalExpense, netProfit }
   - comparison: {
       incomeChangeRate: ((올해-전년)/전년)×100 소수점 1자리,
       expenseChangeRate: 같은 방식,
       profitChangeRate: 같은 방식
     }
3. 전년 데이터가 없으면 (신규 사업자) comparison은 null
4. 기존 응답 구조 유지, comparison 객체만 추가
```

---

## #13. 사업용카드 공제 추천 [세법 검증 완료]

세법 근거:
- 부가가치세법 제46조: 신용카드매출전표 수령 시 매입세액 공제
- 불공제 항목 (부가가치세법 제39조 제1항):
  - 제1호: 접대비 관련 매입세액
  - 제5호: 비영업용 소형승용차

```
프로젝트에서 절세 추천 서비스를 찾아서 읽어줘.
TaxSavingService 또는 비슷한 이름.
기존 추천 항목(노란우산, 연금저축)의 구조 확인.

같은 구조로 "사업용카드 매입세액 공제" 추천을 추가해줘.

세법 근거: 부가가치세법 제46조

추천 로직:
1. BookEntry에서 해당 연도의 사업용(business) 결제 중 카드 결제분 합산
2. 카드 결제 금액의 매입세액(VAT) = 카드결제금액 ÷ 11
   (부가세 포함가 기준: 공급가액 = 결제금액 ÷ 1.1, VAT = 공급가액 × 0.1)
3. 불공제 제외: 접대비(카테고리 17) 관련 매입세액 차감 (부가가치세법 §39①1)
4. estimatedSaving = 공제 가능 매입세액 합계
5. maxAmount = null (사업 관련이면 한도 없음)

추천 텍스트:
- name: "사업용카드 매입세액 공제"
- description: "사업 관련 카드 결제 시 부가세 환급 가능 (부가가치세법 §46). 접대비 관련 매입세액은 불공제 (§39①1)"

기존 추천 항목과 동일한 DTO 구조로 반환.
```

---

## #14. 교육훈련비 공제 추천 [세법 검증 완료]

세법 근거:
- 소득세법 제19조: 사업소득의 필요경비 — 사업 관련 교육비는 경비 인정
- 경비 인정 가능 항목 (1인 IT 개발자):
  - 온라인 강의 (인프런, Udemy) → 연구비(27) 또는 도서인쇄비(25)
  - 기술 서적 → 도서인쇄비(25)
  - 컨퍼런스 참가비 → 연구비(27)
  - 자격증 취득 → 연구비(27)
- 법적 한도: 없음 (사업 관련 합리적 금액이면 전액)
- 디자인 시안 기준 권장 한도: 150만원

주의: 조세특례제한법 제104조의18(중소기업 교육훈련비 세액공제)는 "근로자"에 대한 교육비라서 1인 사업자 본인에게는 직접 적용 안 됨. 여기서 추천하는 건 "필요경비로서의 교육비"임.

```
프로젝트에서 절세 추천 서비스를 읽어줘.
기존 추천 항목 구조 확인.

같은 구조로 "교육훈련비 경비 활용" 추천을 추가해줘.

세법 근거: 소득세법 제19조 (사업소득 필요경비)
사업과 직접 관련된 교육비용은 필요경비로 인정됨.
법적 한도 없음. 권장 목표: 연 150만원 (디자인 시안 기준).

주의: 조세특례제한법 §104의18 (중소기업 교육훈련비 세액공제)는 "근로자" 대상이므로 1인 사업자 본인에게 직접 적용 안 됨. 여기서 추천하는 건 "필요경비"로서의 교육비.

추천 로직:
1. BookEntry에서 해당 연도의 카테고리 25(도서인쇄비) + 27(연구비) 합산 = 현재 사용액
2. 권장 목표: 1,500,000원
3. 잔여 = 1,500,000 - 현재 사용액
4. estimatedSaving = 잔여 × 현재 적용 세율
   (세율은 TaxEstimationService에서 가져오기)
5. 잔여가 0 이하면 추천하지 않음 (이미 충분히 활용 중)

추천 텍스트:
- name: "교육훈련비 경비 활용"
- description: "인프런, Udemy, 기술서적, 컨퍼런스 참가비 모두 경비 처리 가능 (소득세법 §19)"
- 현재 사용액, 권장 한도, 잔여 표시

기존 추천 항목과 동일한 DTO 구조로 반환.
```

---

## #15. 카테고리별 비율 계산 [세법 불필요]

```
프로젝트에서 BookEntryService의 summary 관련 메서드와 응답 DTO를 읽어줘.
특히 byCategory를 반환하는 부분.

CategorySummary(또는 해당 DTO)에 percentage 필드를 추가해줘.

요구사항:
1. percentage = (해당 카테고리 amount / 전체 비용 합계) × 100
2. 소수점 1자리 반올림 (예: 24.1)
3. 수입(INCOME)과 비용(EXPENSE)은 각각 별도 합계 기준으로 비율 계산
4. 전체 합계가 0이면 percentage = 0 (0 나누기 방지)
5. 기존 응답 구조에 percentage 필드만 추가, 나머지 변경 없음
```

---

## 실행 순서 + 예상 시간

```
Day 1 오전 (2시간)
  #1 entryType 필터     — 20분
  #2 날짜 범위 필터      — 20분
  #3 카테고리 필터       — 20분
  #4 거래처 검색         — 15분
  #5 증빙 필터           — 20분
  #6 미분류 필터         — 15분

Day 1 오후 (1.5시간)
  #7 메모 수정 API       — 15분
  #15 비율 계산          — 20분
  #12 전년 대비          — 30분

Day 2 오전 (2시간)
  #13 사업용카드 공제     — 40분
  #14 교육훈련비 공제     — 40분
  #8 지방소득세 CSV      — 30분

Day 2 오후 (1시간)
  #9 날짜 범위 내보내기   — 20분
  #10 Excel 내보내기      — 40분

Day 3 (시간 남으면)
  #11 PDF 장부 리포트     — 2시간
```
